kind: known_issue
id: accuracy-lite-int8-conv1x1-input-pack-batch-missing
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, mindspore-lite, int8, conv1x1, batch-pack]
occurrence_count: 1
affects_platforms: [cpu]
detection:
  pattern: "(Q_language_model_hrmini_Q4_b4_17w|Conv1x1InputPack|端侧C\\+\\+推理精度失败)"
description: |
  Lite int8 inference on Android can show large accuracy drift because the
  underlying `Conv1x1InputPack` path only handles a single batch and ignores
  batch-dimension packing.
issue_refs:
  - issue_id: "42339"
    title: "[Bug]: android_aarch64_cpu_mate40环境，Q_language_model_hrmini_Q4_b4_17w.tflite模型，端侧C++推理精度失败"
    url: "https://gitcode.com/mindspore/org-issues/issues/42339"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "CPU"
    priority: "次要"
    status: "closed"
fix:
  summary: "Add a batch-aware `Conv1x1InputPackBatch` path and switch the int8 conv call sites to it."
  diff: |
    1. Introduce `Conv1x1InputPackBatch` to pack multi-batch int8 Conv1x1
       inputs correctly.
    2. Replace the old single-batch-only packing calls in the int8 convolution
       path with the new batch-aware helper.
    3. Add the golden UT and rerun the original Android-side accuracy test to
       verify the drift is removed.
