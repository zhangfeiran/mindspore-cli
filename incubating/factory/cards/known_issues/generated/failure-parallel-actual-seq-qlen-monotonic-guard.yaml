kind: known_issue
id: failure-parallel-actual-seq-qlen-monotonic-guard
symptom: failure
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-solutiontest, mindspore-org-issues, actual_seq_qlen, mhc, triton, seq32k]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(actual_seq_qlen and actual_seq_kvlen, must be increasing array|seq_length:32k|mhc流程)"
description: |
  DeepSeek 在 `MHC + Triton + seq_length=32k` 并行训练场景下，`actual_seq_qlen/actual_seq_kvlen`
  未满足严格递增且最后一项等于 `T` 的约束，导致训练直接失败。
issue_refs:
  - issue_id: "42432"
    title: "[Bug]: deepseek网络，走mhc流程，使能tirton，seq_length:32k，dp2mp4pp1ep2bs1mb8cp2并行策略，网络训练失败，RuntimeError: For actual_seq_qlen and actual_seq_kvlen, must be increasing array and the last number is equal to T"
    url: "https://gitcode.com/mindspore/org-issues/issues/42432"
    project: "mindspore/org-issues"
    component: "B-SolutionTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "在进入 MHC/Triton 长序列路径前校验并修正 `actual_seq_qlen/kvlen` 的构造逻辑。"
  diff: |
    1. 检查 32k 长序列场景下 `actual_seq_qlen/kvlen` 的生成是否被并行切分或压缩逻辑破坏。
    2. 在框架侧提前校验递增性和末值等于 `T` 的约束，避免把非法数组直接送入底层。
    3. 为 `seq32k + cp/pp/ep + MHC/Triton` 组合补启动回归。
