kind: known_issue
id: function-operator-ctcloss-invalid-arg-crash-guard
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, ctcloss, invalid-arg, crash-guard]
occurrence_count: 1
affects_operators: [ctcloss]
affects_platforms: [ascend]
detection:
  pattern: "(CTCLoss|Segmentation fault|target_lengths = \\(\\)|异常参数)"
description: |
  mindspore/org-issues#42390 is a defensive-programming bug in `mindspore.nn.CTCLoss`.
  The reported repro passes malformed inputs, including an empty tuple for
  `target_lengths`, and the operator crashes instead of returning a validation
  error.
issue_refs:
  - issue_id: "42390"
    title: "[Bug]: Ascend环境 mindspore.nn.CTCLoss算子异常参数场景执行推理发生Segmentation fault"
    url: "https://gitcode.com/mindspore/org-issues/issues/42390"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "次要"
    status: "closed"
fix:
  summary: "Validate `target_lengths` and other CTCLoss inputs before backend dispatch"
  diff: |
    --- a/mindspore/python/mindspore/nn/loss/loss.py
    +++ b/mindspore/python/mindspore/nn/loss/loss.py
    @@
    -return self.ctc_loss(log_probs, targets, input_lengths, target_lengths)
    +_check_ctc_loss_inputs(log_probs, targets, input_lengths, target_lengths)
    +return self.ctc_loss(log_probs, targets, input_lengths, target_lengths)
