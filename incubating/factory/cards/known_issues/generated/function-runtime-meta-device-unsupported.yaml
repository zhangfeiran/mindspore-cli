kind: known_issue
id: function-runtime-meta-device-unsupported
symptom: failure
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, meta, device]
occurrence_count: 2
affects_platforms: [ascend]
detection:
  pattern: "(meta|DeviceCtx\\('meta'\\)|device=meta)"
description: |
  mindspore/org-issues#42372 and #42374 cover the remaining generic meta-device
  runtime failures after the more concrete meta issues were split out. Public
  issue information is still limited here, but both reports indicate that some
  execution paths still fall through to invalid backend/async-copy handling
  instead of either supporting meta tensors or failing early with a dedicated
  guard.
issue_refs:
  - issue_id: "42372"
    title: "[Bug]: device meta使用执行报错：Lazy Async copy failed"
    url: "https://gitcode.com/mindspore/org-issues/issues/42372"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
  - issue_id: "42374"
    title: "[Bug]: device meta特性执行报错：RuntimeError: Backend type is invalid, should be one of [CPU, GPU, Ascend], but got meta"
    url: "https://gitcode.com/mindspore/org-issues/issues/42374"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Keep a residual card for remaining generic meta backend/copy failures that still need issue-specific extraction"
  diff: |
    Public issue details are still insufficient to extract a single precise
    source diff for these two residual reports. The actionable direction is:
    1. intercept invalid backend/copy paths before they touch real device code
    2. add explicit unsupported-on-meta guards where support is still absent
    3. split this residual card again once the issue comments expose concrete
       operator- or module-level fixes
