kind: known_issue
id: accuracy-parallel-ring-attention-bsh-splitfull-drift
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, ring-attention, semi-auto-parallel, bsh]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(ring-attention.*layout is BSH.*split full|与单卡差距甚大)"
description: |
  在 `semi_auto_parallel` 下，Ring Attention 使用 `BSH` layout 且 `split full` 时，
  结果与单卡基线偏差明显，属于并行切分语义错误而不是普通数值波动。
issue_refs:
  - issue_id: "42415"
    title: "[Bug]: in semi_auto_parallel on ring-attention and layout is BSH,split full， 计算结果与单卡差距甚大"
    url: "https://gitcode.com/mindspore/org-issues/issues/42415"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "核对 Ring Attention 在 `BSH + split full` 组合下的切分与重组语义，恢复与单卡一致。"
  diff: |
    1. 固定单卡与 `semi_auto_parallel` 的相同输入，比较 `BSH` layout 下各阶段结果。
    2. 检查 `split full` 的 shape 映射、通信和输出重组是否与 ring-attention 假设一致。
    3. 为 `BSH + split full` 组合补精度回归。
