kind: known_issue
id: function-runtime-device-config-invalid
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [mindformerstest, mindspore-org-issues, device-config, runtime]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(aclrtSetDevice failed|device id is valid)"
description: |
  mindspore/org-issues#42418 originally looked like a framework-side device
  configuration bug because multiple cases failed at `aclrtSetDevice`.
  However, the public issue comments explicitly conclude this was an
  environment problem: the machine had dropped a device (`掉卡`), and the cases
  passed after switching to a healthy environment.
issue_refs:
  - issue_id: "42418"
    title: "[Bug]: 【B080】多条用例报错：RuntimeError: Call aclrtSetDevice failed, ret[507033]. Got device count[16] and device id[7], please check if device id is valid"
    url: "https://gitcode.com/mindspore/org-issues/issues/42418"
    project: "mindspore/org-issues"
    component: "MindFormersTest"
    backend: "Ascend"
    priority: "严重"
    status: "closed"
fix:
  summary: "Environment-side issue only; move this scene out of framework fix recommendations"
  diff: |
    Public issue comments state the real root cause is device loss in the test
    environment, not a code bug. This card should eventually be replaced by the
    more precise environment-oriented no-diff card.
