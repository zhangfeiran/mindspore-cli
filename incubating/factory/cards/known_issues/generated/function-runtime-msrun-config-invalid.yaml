kind: known_issue
id: function-runtime-msrun-config-invalid
symptom: failure
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, msrun, config, b-sig-parallel, mindspore-hyper-parallel, b-solutiontest, mindformerstest]
occurrence_count: 3
affects_platforms: [ascend]
detection:
  pattern: "(bind_numa|绑核|localrank|局部rank|msrun)"
description: |
  Residual `msrun` / bind-affinity config card after splitting out the clearer
  YAML-dict compatibility issue and documentation-only JSON sample issue. The
  remaining public issues still mix three partially related areas:
  invalid `--bind_numa` CLI argument handling, local-rank retrieval in
  hyper-parallel, and JSON affinity compatibility/logging behavior.
issue_refs:
  - issue_id: "42212"
    title: "[Bug]: msrun启动--bind_numa参数传入不存在路径报错msrun: error: argument --bind_numa"
    url: "https://gitcode.com/mindspore/org-issues/issues/42212"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
  - issue_id: "25"
    title: "【BUG】进程组获取局部rank"
    url: "https://gitcode.com/mindspore/hyper-parallel/issues/25"
    project: "mindspore/hyper-parallel"
    component: "B-SIG-Parallel"
    backend: "Ascend"
    priority: "无优先级"
    status: "closed"
  - issue_id: "42348"
    title: "【MindFormers】【新特性】【B060】【910B4】绑核支持json配置传入的方案和yaml配置方案差异过大，易用性差"
    url: "https://gitcode.com/mindspore/org-issues/issues/42348"
    project: "mindspore/org-issues"
    component: "MindFormersTest"
    backend: "Ascend"
    priority: "次要"
    status: "closed"
fix:
  summary: "Residual `msrun`/affinity config card pending finer split by CLI, local-rank, and JSON-affinity behavior"
  diff: |
    The remaining issues do not share one concrete patch. Current public
    guidance suggests three separate directions:
    1. validate `--bind_numa` inputs earlier in the CLI
    2. stabilize local-rank retrieval behavior in hyper-parallel
    3. keep JSON affinity config compatibility and explicit error reporting
