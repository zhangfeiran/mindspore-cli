kind: known_issue
id: accuracy-operator-clamp-precision-drift
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, precision-drift, clamp]
occurrence_count: 1
affects_operators: [clamp]
affects_platforms: [ascend]
detection:
  pattern: "(精度|异常值|不一致|nan|inf|loss|accuracy)"
description: |
  mindspore/org-issues#42271 reports `Tensor.clamp` mismatches only in O1 mode.
  The issue comments attribute the regression to a DVM-side `Cmp` fusion bug in
  the O1 path, not to clamp arithmetic itself.
issue_refs:
  - issue_id: "42271"
    title: "[Bug]: Tensor.clamp O1模式下计算结果与标杆不一致"
    url: "https://gitcode.com/mindspore/org-issues/issues/42271"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Fix the DVM-side O1 Cmp fusion path instead of patching clamp"
  diff: |
    --- a/dvm/o1/cmp_fusion_pass.cc
    +++ b/dvm/o1/cmp_fusion_pass.cc
    @@
    -old cmp fusion behavior
    +fixed cmp fusion behavior for O1 clamp scene
    Notes:
    - Issue comment explicitly says "O1模式 Cmp融合算子存在问题".
    - The implementation change is referenced from PR #91899.
