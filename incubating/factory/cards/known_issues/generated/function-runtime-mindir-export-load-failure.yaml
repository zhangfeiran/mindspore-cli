kind: known_issue
id: function-runtime-mindir-export-load-failure
symptom: failure
severity: medium
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-sig-innerdelivery, mindspore-mindspore-lite, mindir, export-load, b-componenttest, mindspore-org-issues]
occurrence_count: 1
affects_platforms: [cpu]
detection:
  pattern: "(mindir|导出MINDIR|导出mindir)"
description: |
  Residual MINDIR export/load card after splitting out the clearer Lite
  environment-path conflict and micro null-context bug. The remaining public
  issue in this cluster is `thermal_sdae.mindir` micro training with
  `MSModelBuildFromFile failed, ret: -600`, which still lacks enough public
  repair detail to extract a precise code diff.
issue_refs:
  - issue_id: "42413"
    title: "[Bug]:  thermal_sdae.mindir模型执行micro训练，报错MSModelBuildFromFile failed, ret: -600"
    url: "https://gitcode.com/mindspore/org-issues/issues/42413"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "CPU"
    priority: "次要"
    status: "opened"
fix:
  summary: "Residual micro MINDIR load card pending more public fix details for the `ret:-600` scene"
  diff: |
    Public material is still insufficient to extract a single precise fix for
    `MSModelBuildFromFile failed, ret: -600`. Keep this card narrow until the
    corresponding issue comments expose the real root cause and patch.
