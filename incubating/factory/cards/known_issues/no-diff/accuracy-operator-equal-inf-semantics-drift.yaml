kind: known_issue
id: accuracy-operator-equal-inf-semantics-drift
symptom: accuracy
severity: medium
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, inf-semantics-drift, equal]
occurrence_count: 1
affects_operators: [equal]
affects_platforms: [ascend]
detection:
  pattern: "(精度|异常值|不一致|nan|inf|loss|accuracy)"
description: |
  mindspore/org-issues#41903 reports that `mint.equal/tensor.equal` returns
  False for two `inf` inputs after a CANN upgrade. The issue comments confirm
  this is a CANN-side regression introduced while fixing earlier precision
  problems, and the testcase passes again with a newer CANN package.
issue_refs:
  - issue_id: "41903"
    title: "[BUG][OPS] CANN包升级8.5 0105, mint.equal/tensor.equal在两个输入都为inf值时输出结果为False，与此前相反"
    url: "https://gitcode.com/mindspore/org-issues/issues/41903"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "次要"
    status: "closed"
fix:
  summary: "Environment-side fix only; no public framework source diff extracted"
  diff: |
    Issue comments identify this as a CANN regression and say the testcase
    passes after upgrading to a newer CANN build. No public framework-side
    source patch is exposed, so this card is kept under `no-diff`.
