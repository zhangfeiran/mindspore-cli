kind: known_issue
id: function-runtime-lite-divs-adapter-missing
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, lite, adapter, divs]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(Cannot get adapter for .*Divs|Divs-op2)"
description: |
  mindspore/org-issues#42308 reports a Lite conversion failure on a graph that
  contains `Divs`. Earlier triage notes indicate the path is tied to the `/`
  overload lowering to `Divs`, while the Lite side lacks a matching adapter.
  The source patch itself is not available in the public issue payload.
issue_refs:
  - issue_id: "42308"
    title: "[Bug]: refiner模块执行转换流程报错：Cannot get adapter for Default/pos_embed-FluxPosEmbed/Divs-op2"
    url: "https://gitcode.com/mindspore/org-issues/issues/42308"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Lite adapter gap for `Divs`; public issue exposes workaround context but not the merged patch"
  diff: |
    Triage notes associate this with `/` lowering to `Divs` while Lite lacks the
    corresponding adapter. Keep as `no-diff` until the exact source change is
    recovered and verified.
