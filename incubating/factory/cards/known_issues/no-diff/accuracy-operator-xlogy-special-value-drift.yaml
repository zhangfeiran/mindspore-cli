kind: known_issue
id: accuracy-operator-xlogy-special-value-drift
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, special-value-drift, xlogy]
occurrence_count: 1
affects_operators: [xlogy]
affects_platforms: [ascend]
detection:
  pattern: "(精度|异常值|不一致|nan|inf|loss|accuracy)"
description: |
  mindspore/org-issues#42299 reports that `mint.xlogy` disagrees with torch and
  numpy when `input` is finite and `other` is `np.nan`. The available issue
  comments do not expose a concrete code patch; they only show that the scene
  passes on newer CANN / MindSpore versions. There is not enough public detail
  to write a trustworthy source-level diff from the issue alone.
issue_refs:
  - issue_id: "42299"
    title: "[Bug]:[mint.xlogy]input是正常数值，other是np.nan 结果与torch和numpy不一样"
    url: "https://gitcode.com/mindspore/org-issues/issues/42299"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "No public source-level fix diff extracted yet; observed as fixed in newer package combinations"
  diff: |
    Public issue comments only confirm that the testcase passes on newer
    MindSpore/CANN versions. No root-cause comment or linked patch was exposed
    in the issue discussion, so this card should be moved to `no-diff` if a
    stricter curation policy is required.
