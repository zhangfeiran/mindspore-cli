kind: known_issue
id: accuracy-operator-addmm-precision-drift
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, precision-drift, addmm]
occurrence_count: 1
affects_operators: [addmm]
affects_platforms: [ascend]
detection:
  pattern: "(精度|异常值|不一致|nan|inf|loss|accuracy)"
description: |
  mindspore/org-issues#42264 reports an Ascend `Tensor.addmm` mismatch after a
  CANN change. The discussion classifies this as a CANN-side regression rather
  than a framework operator bug. Validation passed after switching to the fixed
  CANN package, so the effective action is environment replacement, not `addmm`
  kernel modification.
issue_refs:
  - issue_id: "42264"
    title: "[Bug]: Tensor.addmm ascend后端计算结果与标杆不一致"
    url: "https://gitcode.com/mindspore/org-issues/issues/42264"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Environment-side fix only; no public framework source diff extracted"
  diff: |
    Issue comments only state that this is a CANN problem and the scene passes
    after switching to a fixed CANN package. No public framework-side source
    patch is exposed, so this card is kept under `no-diff`.
