kind: known_issue
id: accuracy-inference-asr-topk-indices-mismatch
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, asr, inference, topk]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(ASR模型推理|topk_indices|prefill|decode)"
description: |
  ASR prefill/decode inference can fail accuracy checks mainly on `topk_indices`
  while other outputs stay within tolerance. Public issue comments only show the
  symptom and later regression-pass result; they do not expose the exact source
  patch or root-cause analysis.
issue_refs:
  - issue_id: "42326"
    title: "[Bug]: ASR模型推理，精度不符合预期"
    url: "https://gitcode.com/mindspore/org-issues/issues/42326"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Public discussion confirms the regression later passed, but does not expose a trustworthy code-level fix summary."
  diff: |
    The issue thread contains regression-pass output for the ASR prefill/decode
    models, but no public root-cause narrative, PR link, or code-diff summary.
    Keep this card under `no-diff` until the actual fix commit or internal
    analysis is available.
