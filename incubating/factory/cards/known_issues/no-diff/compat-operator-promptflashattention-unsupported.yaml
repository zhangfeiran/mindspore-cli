kind: known_issue
id: compat-operator-promptflashattention-unsupported
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, lite, onnx, ascend, promptflashattention]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(PromptFlashAttention|Unsupported_Operator|converter_lite)"
description: |
  mindspore/org-issues#42318 fails during ONNX-to-MINDIR conversion because the
  Ascend Lite converter cannot select a kernel for `PromptFlashAttention`.
  The public issue body only exposes the unsupported-operator diagnostics, so
  this is currently tracked as a compatibility limitation rather than a card
  with verified source diff.
issue_refs:
  - issue_id: "42318"
    title: "[Bug]: facesr_di_unet_1030_fp16.onnx模型转换，预期成功，实际转换失败"
    url: "https://gitcode.com/mindspore/org-issues/issues/42318"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Ascend Lite lacks usable `PromptFlashAttention` support for this graph; no merged source diff extracted"
  diff: |
    The public issue only shows converter diagnostics:
    `Unsupported_Operator(EZ3002): Optype [PromptFlashAttention] ... is unsupported`.
