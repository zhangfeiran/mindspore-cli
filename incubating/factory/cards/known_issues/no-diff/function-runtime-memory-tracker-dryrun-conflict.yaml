kind: known_issue
id: function-runtime-memory-tracker-dryrun-conflict
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-solutiontest, mindspore-org-issues, runtime, memory-tracker, dryrun, config]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(memory_tracker:True|MS_ALLOC_CONF|dryrun)"
description: |
  mindspore/org-issues#42337 reports that a DeepSeek dryrun launches normally
  without `memory_tracker`, but fails once `MS_ALLOC_CONF=\"...memory_tracker:True\"`
  is added. The public issue body gives the exact trigger combination, but no
  source-level repair diff is available from the issue itself.
issue_refs:
  - issue_id: "42337"
    title: "[Bug]: dryrun跑测deepseek-mcore网络，加上memory_tracker:True执行报错，不加能正常跑"
    url: "https://gitcode.com/mindspore/org-issues/issues/42337"
    project: "mindspore/org-issues"
    component: "B-SolutionTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Unsupported dryrun plus memory-tracker combination; no verified source diff extracted yet"
  diff: |
    Reproduced trigger from the public issue:
    `MS_SIMULATION_LEVEL=3` together with `MS_ALLOC_CONF=...memory_tracker:True`.
    The issue text does not expose the merged code change, so this card remains
    in `no-diff` until the actual guard or compatibility patch is recovered.
