kind: known_issue
id: compat-operator-avgpool-backward-custom-aicpu-bin-missing
symptom: failure
severity: medium
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-solutiontest, mindspore-org-issues, aclnn, avgpool, aicpu, bin-missing]
occurrence_count: 1
affects_operators: [avgpool1d, avgpool2d, avgpool3d]
affects_platforms: [ascend]
detection:
  pattern: "(aclnnAvgPool2dBackwardGetWorkspaceSize call failed|Get cust aicpu kernel bin from attr cust_aicpu_kernel failed|SyncCopy failed)"
description: |
  mindspore/org-issues#42235 aggregates AvgPool backward failures after CANN
  upgrade. The public issue body points to custom AICPU kernel bin lookup
  failure plus `SyncCopy failed`, which is more specific than a generic
  workspace error but still does not expose the merged repair diff.
issue_refs:
  - issue_id: "42235"
    title: "[Bug]: [OPS][CANN升级]算子执行出现报错：RuntimeError: aclnnAvgPool2dBackwardGetWorkspaceSize call failed"
    url: "https://gitcode.com/mindspore/org-issues/issues/42235"
    project: "mindspore/org-issues"
    component: "B-SolutionTest"
    backend: "Ascend"
    priority: "次要"
    status: "closed"
fix:
  summary: "CANN custom AICPU kernel packaging/integration issue; no verified source diff extracted"
  diff: |
    Public diagnostics:
    `Get cust aicpu kernel bin from attr cust_aicpu_kernel failed`
    and `SyncCopy failed`.
