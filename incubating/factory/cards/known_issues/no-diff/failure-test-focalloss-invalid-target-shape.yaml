kind: known_issue
id: failure-test-focalloss-invalid-target-shape
symptom: failure
severity: medium
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-sig-innerdelivery, mindspore-org-issues, test, focalloss, target-shape]
occurrence_count: 1
affects_operators: [focalloss]
affects_platforms: [ascend]
detection:
  pattern: "(ground truth has different shape from input|FocalLoss|target.*different shape)"
description: |
  mindspore/org-issues#42358 is best classified as a test-input problem. The
  public issue body shows `predict` with shape `(2, 3, 7, 8)` but `target` with
  shape `(2, 3, 28, 2)`, which violates MONAI `FocalLoss`'s documented contract
  before any framework-specific behavior is considered.
issue_refs:
  - issue_id: "42358"
    title: "[Bug]: [兼容性测试]focalloss报错ground truth has different shape from input"
    url: "https://gitcode.com/mindspore/org-issues/issues/42358"
    project: "mindspore/org-issues"
    component: "B-SIG-InnerDelivery"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Invalid comparison case; no framework source diff should be extracted from this issue"
  diff: |
    The public reproduction already feeds an invalid target shape to FocalLoss.
    This should be fixed in the compatibility case, not turned into a framework
    operator patch.
