kind: known_issue
id: failure-runtime-mac-pynative-amp-o0-segfault
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, mac, pynative, amp, segfault]
occurrence_count: 1
affects_platforms: [cpu]
detection:
  pattern: "(mac.*pynative.*auto_mixed_precision.*O0.*segmentation fault|build_train_network)"
description: |
  mindspore/org-issues#42272 reports a macOS ARM CPU crash when
  `amp.build_train_network(..., level='O0')` is used in pynative ResNet50
  training. The issue is still open and the public payload does not expose a
  merged fix.
issue_refs:
  - issue_id: "42272"
    title: "[Bug]: mac pynative模式，自动混合精度，调用build_train_network接口，跑resnet50网络，auto_mixed_precision输入为”O0”segmentation fault"
    url: "https://gitcode.com/mindspore/org-issues/issues/42272"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "CPU"
    priority: "主要"
    status: "opened"
fix:
  summary: "Open crash report only; keep as scenario card until the actual fix lands"
  diff: |
    No authoritative source diff is available because the issue remains open.
