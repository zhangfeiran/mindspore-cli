kind: known_issue
id: failure-runtime-dynamic-msfunction-mindir-cpu-timeout
symptom: failure
severity: medium
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, timeout, ms_function, dynamic-shape, mindir, cpu]
occurrence_count: 1
affects_platforms: [cpu]
detection:
  pattern: "(dynamic_shape_msfunc_singlecell_0001_mindir|timeout\\(150\\)|Mindir compare)"
description: |
  mindspore/org-issues#42140 is a CPU timeout in a dynamic-shape training plus
  MINDIR export/load validation case under pynative with `@ms_function`.
  The public issue body contains the harness and timeout symptom, but not the
  exact framework patch.
issue_refs:
  - issue_id: "42140"
    title: "[Bug]: 网络单个/几个cell@ms_function，pynative模式下，在该网络下，进行动态shape的网络训练，导出mindir并加载,在CPU后端执行用例超时报错"
    url: "https://gitcode.com/mindspore/org-issues/issues/42140"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "CPU"
    priority: "主要"
    status: "closed"
fix:
  summary: "Long-running composite timeout case retained, but no authoritative source diff extracted"
  diff: |
    Public issue data is sufficient for scenario recognition only; the actual
    performance or control-flow fix is not visible.
