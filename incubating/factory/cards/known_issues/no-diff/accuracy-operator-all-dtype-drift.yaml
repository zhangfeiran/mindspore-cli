kind: known_issue
id: accuracy-operator-all-dtype-drift
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, dtype-drift, all]
occurrence_count: 1
affects_operators: [all]
affects_platforms: [ascend]
detection:
  pattern: "(精度|异常值|不一致|nan|inf|loss|accuracy)"
description: |
  mindspore/org-issues#42442 reports `mint.all` mismatches for bfloat16 on
  Ascend 910B. The issue comments identify the bug inside the CANN `aclnnAll`
  interface for float16/float32/bfloat16 and say the scene passes after moving
  to the fixed CANN B109 build.
issue_refs:
  - issue_id: "42442"
    title: "[Bug]: mint.all bfloat16 910B后端输出结果与标杆不一致"
    url: "https://gitcode.com/mindspore/org-issues/issues/42442"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Environment-side fix only; no public framework source diff extracted"
  diff: |
    Issue comments say the bug is inside `aclnnAll` and is fixed by upgrading
    from the affected CANN build to B109. No public framework-side source patch
    is exposed, so this card is kept under `no-diff`.
