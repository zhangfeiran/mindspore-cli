kind: known_issue
id: accuracy-operator-reciprocal-inf-semantics-drift
symptom: accuracy
severity: high
lifecycle:
  state: draft
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, accuracy, inf-semantics-drift, reciprocal]
occurrence_count: 1
affects_operators: [reciprocal]
affects_platforms: [ascend]
detection:
  pattern: "(精度|异常值|不一致|nan|inf|loss|accuracy)"
description: |
  mindspore/org-issues#42294 reports a complex64 `inf` mismatch for
  `Tensor.reciprocal` on non-empty shapes. The issue discussion says this path
  goes through an AICPU implementation whose historical `nan`/`inf` behavior is
  inconsistent with torch but consistent with tensorflow. The final conclusion
  marks it as a non-issue for non-network scenarios and recommends skipping the
  testcase scene with an explanatory comment.
issue_refs:
  - issue_id: "42294"
    title: "[Bug]: Tensor.reciprocal：对非空shape的complex64的inf支持有问题,与torch对比不符"
    url: "https://gitcode.com/mindspore/org-issues/issues/42294"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Non-issue semantics divergence; no reusable source-level fix extracted"
  diff: |
    Issue comments conclude this complex64 inf scene is a non-issue and should
    simply be skipped or annotated. Since no stable source-level fix is exposed,
    this card is kept under `no-diff`.
