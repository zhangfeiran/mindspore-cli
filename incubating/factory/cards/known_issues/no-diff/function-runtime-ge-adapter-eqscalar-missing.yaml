kind: known_issue
id: function-runtime-ge-adapter-eqscalar-missing
symptom: failure
severity: high
lifecycle:
  state: stable
source:
  kind: manual
confidence:
  level: observed
tags: [b-componenttest, mindspore-org-issues, ge, adapter, eqscalar]
occurrence_count: 1
affects_platforms: [ascend]
detection:
  pattern: "(Cannot get adapter for .*EqScalar|EqScalar-op0)"
description: |
  mindspore/org-issues#42307 reports an inference graph conversion failure in
  GE: `Cannot get adapter for ... EqScalar-op0`. The public issue body clearly
  identifies the missing adapter symptom, but the merged implementation fix is
  not exposed in the issue data we can read.
issue_refs:
  - issue_id: "42307"
    title: "[Bug]: DIT模块执行推理流程失败，报错：Cannot get adapter for Default/context_embedders-CellList/0-HunyuanVideoTokenRefiner/token_refiner-HunyuanVideoIndividualTokenRefiner/EqScalar-op0"
    url: "https://gitcode.com/mindspore/org-issues/issues/42307"
    project: "mindspore/org-issues"
    component: "B-ComponentTest"
    backend: "Ascend"
    priority: "主要"
    status: "closed"
fix:
  summary: "Missing GE adapter for `EqScalar`; no authoritative merged source diff extracted"
  diff: |
    Public evidence is limited to the failing GE conversion log. This card is
    kept in `no-diff` until the actual adapter registration patch is recovered.
